# Evidence Handoff

The n8n execution evidence is already captured locally by the project owner.

Place the four screenshots in `evidence/` using these filenames:

- `test-a-automatic-processing.png`
- `test-b-delivery-not-received.png`
- `test-c-source-mismatch.png`
- `test-d-unknown-order.png`

The screenshots should show the n8n execution output, not just the input form.

The current project pack already contains the text test evidence and the workflow export supplied during the session.
